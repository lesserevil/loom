# Loom — environment variables
# Copy this to .env for local development. Never commit .env.
#
# USER-SCOPED SECRETS (go in ~/.loom/config.env, NOT here):
#   TOKENHUB_VAULT_PASSWORD  — vault password for embedded TokenHub
#   TOKENHUB_ADMIN_TOKEN     — leave unset to auto-generate on each restart
#
# See ~/.loom/config.env for the format (create it if absent).

# Git tokens for agent repo access
GITLAB_TOKEN=
GITHUB_TOKEN=

# Optional: password for the Loom web UI
# LOOM_PASSWORD=

# Optional: your TokenHub API key (registered via the TokenHub admin UI or bootstrap.local)
# TOKENHUB_API_KEY=

# Optional: extra OpenAI-compatible providers to inject into TokenHub at startup
# TOKENHUB_EXTRA_PROVIDERS=
